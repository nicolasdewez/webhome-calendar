{% extends '@NdewezWebHomeCommon/base.html.twig' %}

{% import "@NdewezWebHomeCommon/macros/forms.html.twig" as forms %}

{% block title %}{{ parent() }} - {{ 'jobs.title.edit'|trans }}{% endblock %}

{% block breadcrumb %}
    {{ include('NdewezWebHomeCommonBundle::breadcrumb.thml.twig', {
            'links': [
                {'title': 'home.title.home', 'route': 'app_home'},
                {'title': 'jobs.title.list', 'route': 'app_jobs_list'},
                {'title': 'jobs.title.edit', 'active': true},
            ]
        })
    }}
{% endblock %}

{% block body %}

    <h2>{{ 'jobs.title.edit'|trans }}</h2>

    {{ form_start(form) }}
        {{ form_row(form.code)}}
        {{ form_row(form.title)}}
        {{ form_row(form.startTime)}}
        {{ form_row(form.endTime)}}
        {{ form_row(form.active)}}

        {{ forms.buttons(form, ['submit', 'delete']) }}
    {{ form_end(form) }}

{% endblock %}
